export enum SLOTS {
  HEADER = "header",
  FOOTER = "footer",
  DEFAULT = "default",
}

export enum ATTRS {
  ITEM_UNIQUE = 'data-z-scroller-chat'
}

export enum SCROLL_DIRECTION {
  FRONT = 'front',
  BEHIND = 'behind'
}